import type { DeletionMode } from '../types'

export const NONE = 'none' satisfies DeletionMode
export const TOOLTIP = 'tooltip' satisfies DeletionMode
export const DROP = 'drop' satisfies DeletionMode
